import Registration from '@/components/registration/registration';
import React from 'react';

const page = () => {
    return (
        <div>
            <Registration />
        </div>
    );
};

export default page;